@model IEnumerable<HotelManagement.Models.RoomsModel>

@{
    ViewData["Title"] = "bookRooms";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.roomid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.roomnumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.roomtype)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.imagepath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.facilities)
            </th>
            <th>
               Cost per Day($)
            </th>
         <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.roomid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.roomnumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.roomtype)
            </td>
            <td>
                    <img src="~/roomimages/@item.imagepath" style="height:250px" />

            </td>
            <td>
                  @Html.DisplayFor(modelItem=>item.facilities)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cost)
            </td>  
            <td>
                <div class="button">
                    @Html.ActionLink("Book Room", "bookroom", new { roomid = item.roomid,cost=item.cost })
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
